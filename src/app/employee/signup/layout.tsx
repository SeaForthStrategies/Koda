export const metadata = {
  title: "Create Employee Account – Koda",
  description: "Create your Koda employee account",
};

export default function EmployeeSignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
