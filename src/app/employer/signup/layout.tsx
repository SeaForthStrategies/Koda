export const metadata = {
  title: "Create Employer Account – Koda",
  description: "Create your Koda employer account",
};

export default function EmployerSignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
